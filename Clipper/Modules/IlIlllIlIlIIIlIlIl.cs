using System;
using System.Threading;

namespace Google.TOKENSs
{
	internal class IlIlllIlIlIIIlIlIl
	{
		public static void Check()
		{
			bool flag = false;
			Mutex mutex = new Mutex(false, ڋ۶ﻌۅڑڋږٱۋۓګ۶ٷګﺁ.mutex, out flag);
			bool flag2 = !flag;
			bool flag3 = flag2;
			if (flag3)
			{
				Environment.Exit(1);
			}
		}
	}
}