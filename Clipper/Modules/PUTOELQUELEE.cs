using System;
using System.Collections.Generic;
using System.Text.RegularExpressions;

namespace Google.TOKENSs
{
	internal sealed class PUTOELQUELEE
	{
		public static Dictionary<string, Regex> patterns = new Dictionary<string, Regex>
		{
			{
				TOKENS.ڔﺈﻌﺇﻬ(),
				new Regex(TOKENS.ڿۋٷۓﻬ())
			},
			{
				TOKENS.ڔږﻌﻬڋ(),
				new Regex(TOKENS.ﭢڙﻐٱګ())
			},
			{
				TOKENS.ڕږڋﺂٷ(),
				new Regex(TOKENS.ڙڔﻬﺁﻲ())
			},
			{
				TOKENS.ﭢﺈﺂ۶ے(),
				new Regex(TOKENS.ڙڕڋﺂﻲ())
			}
		};
	}
}
